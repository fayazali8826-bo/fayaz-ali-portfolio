{
  "buildCommand": "cd fayaz-ali-portfolio && npm install && npm run build",
  "outputDirectory": "fayaz-ali-portfolio/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
